node_modules/
target/
dist/
web-dist/
.vite/
.vite-task/
.turbo/
.cache/
.tmp/
coverage/
*.log
# Local secrets. Provider credentials may exist here, but repo tools must not
# read, print, expose, or commit them.
.env
.env.*
!.env.example
!.env.*.example
# Local-only purchased-game corpora and private benchmark inputs.
fixtures/private-local/
.kaifuu/secrets.local/
# Local research scratch exports. Curated research notes live under docs/research/.
/research/
# Private full-fidelity render artifacts (real decoded g00 pixels). NEVER
# committed: the render-validate emit boundary writes copyrighted game-art
# frames here for local inspection only. Public/committed frames are redacted.
/.private-render/
# Per-agent worktrees managed by Claude Code agents.
.claude/

# qdcli — repo-local sqlite cache; rebuild via `qd import --from roadmap/spec-dag.json --schema-mapping roadmap/qd-import-map.json`
.qd/qd.db
.qd/qd.db-wal
.qd/qd.db-shm
.qd/logs/
artifacts/
