# Itotori App

The Itotori app owns localization state: bridge import, locale branches, draft generation, deterministic checks, patch-ready export, runtime evidence ingestion, and future feedback workflows.

The current scaffold uses a deterministic fake provider so `just hello` can run without external model credentials.
