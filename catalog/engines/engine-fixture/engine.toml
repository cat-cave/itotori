schema = "engine/v1"
id = "engine-fixture"
plugin = "bin/kaifuu-cli"

[capabilities]
asset_inventory = "supported"
asset_listing = "supported"
asset_text_patching = "not-claimed"
codec_access = "supported"
container_access = "supported"
crypto_access = "supported"
delta_patching = "not-claimed"
detection = "supported"
encrypted_input = "not-claimed"
extraction = "supported"
key_profile = "not-claimed"
line_parity_patching = "partial"
non_text_surface_extraction = "partial"
patch_back = "partial"
patching = "partial"
profile_generation = "supported"
runtime_vm = "not-claimed"
verification = "supported"

[[check]]
id = "check-fixture-public-capabilities"
lane = "lane-public"
covers = [
  "asset_inventory",
  "asset_listing",
  "codec_access",
  "container_access",
  "crypto_access",
  "detection",
  "extraction",
  "line_parity_patching",
  "non_text_surface_extraction",
  "patch_back",
  "patching",
  "profile_generation",
  "verification",
]
executor = { kind = "engine-plugin", command = "test", selector = "synthetic-capabilities" }

[[proof]]
id = "proof-fixture-strict-capabilities"
lane = "lane-strict"
covers = [
  "asset_inventory",
  "asset_listing",
  "codec_access",
  "container_access",
  "crypto_access",
  "detection",
  "extraction",
  "line_parity_patching",
  "non_text_surface_extraction",
  "patch_back",
  "patching",
  "profile_generation",
  "verification",
]
requires = { corpus_count = 0, tags = [] }
executor = { kind = "engine-plugin", command = "prove", selector = "synthetic-capabilities" }
receipt = { outcome = "synthetic-capability-parity", minimum_executed = 1 }
