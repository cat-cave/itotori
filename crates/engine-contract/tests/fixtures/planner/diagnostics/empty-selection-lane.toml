schema = "lane/v1"
id = "lane-empty"
class = "periodic"
selector = "proof.lane == 'lane-empty'"
requirements = ["private-inventory"]
empty_plan = "fail"
missing_requirement = "fail"
unexecuted_selection = "fail"
receipt = "required"
