schema = "lane/v1"
id = "lane-strict"
class = "periodic"
selector = "proof.lane == 'lane-strict'"
requirements = ["private-inventory", "read-only-inputs", "renderer"]
empty_plan = "fail"
missing_requirement = "fail"
unexecuted_selection = "fail"
receipt = "required"
