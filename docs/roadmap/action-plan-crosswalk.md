# Action-plan reconciliation

[`docs/action-plan.md`](../action-plan.md) remains the sole intent authority.
This roadmap changes the unit of implementation without changing product
scope. A spec is a semantic bundle with exact behavior-cell exits; GitHub
blocked-by relationships carry the transitively reduced dependency graph.

## Non-negotiable decisions retained

| Action-plan decision                                                                                                      | Roadmap enforcement                                                                                                                                                                                                                       |
| ------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Adapter-owned identity and proof topology; no family lists in orchestration, recipes, workflows, or environment variables | `proof-ledger-and-explicit-failures` owns generated selection and receipts; `compatibility-and-studio-discovery` and every family expansion consume the canonical adapter registry.                                                       |
| The first complete positive production vertical is RealLive                                                               | The `decode.engine.reallive` instances of the five `admitted-*` bundles own the first end-to-end admission. `afterFirstProduction` edges make that result the prerequisite for shared runtime publication and later family qualification. |
| Two independent lawful titles are required for a positive family claim                                                    | Extraction, patch/reproduction, runtime, and played/export journey acceptance all require a two-title quorum. Synthetic and bounded-role rows cannot satisfy it.                                                                          |
| Full-fidelity evidence stays private; only non-reconstructive derivatives are public                                      | `privacy-and-commit-authority`, `safe-runtime-proof-publication`, the family journey bundles, and [`real-bytes.md`](real-bytes.md) separate encrypted private evidence from signed content-free receipts.                                 |
| The deployment registry contains exactly eight inputs                                                                     | `deployment-and-clean-host` retains the source guard and rejects a ninth input. Corpus identity, locale, revision, scope, and budget remain durable application or run data.                                                              |
| Missing runner, input, or tool; skip; empty selection; zero execution; or selected/executed mismatch fails                | `proof-ledger-and-explicit-failures` makes every condition an explicit red result and closes both failure and portable-evidence cells.                                                                                                    |
| Canonical scope is exactly 582 unique source identities                                                                   | [`source-accounting.md`](source-accounting.md) closes the identity lineage and retires prose-derived totals. `proof-ledger-and-explicit-failures` preserves the exact source/map join, states, dispositions, and pinned hashes.           |
| No undeclared compatibility shims                                                                                         | `public-formats-and-artifact-lineage` permits only declared version negotiation, migration, and retirement, and rejects incompatible versions.                                                                                            |
| Agents own the inner localization loop                                                                                    | `run-policy-accounting-and-entrypoints`, `durable-control-and-state`, and each family localization/patch/reproduction bundle require complete agent disposition and forbid human unit decisions.                                          |
| Humans act only through whole played-patch rounds and played export                                                       | Family played/export journey bundles and `whole-round-refinement-and-comparison` require the exact immutable play receipt and expose no unit-edit worklist.                                                                               |
| Detection grants no downstream support                                                                                    | `compatibility-and-studio-discovery` discloses bounded evidence; only the family qualification cell in a played/export journey can admit claimed operations.                                                                              |
| Clean-host, local/managed custody, and portable release remain in scope                                                   | `deployment-and-clean-host`, `privacy-and-commit-authority`, `safe-runtime-proof-publication`, and the final family journeys compose self-hosted, managed-control/local-executor, and managed-confidential contracts.                     |

## Workstream crosswalk

| Action-plan workstream                                   | Owning semantic bundles                                                                                                                                                                                                                                                                    |
| -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Package-owned family identity                            | `proof-ledger-and-explicit-failures` proves concurrent registry-derived planning and receipt identity. `compatibility-and-studio-discovery`, all family intake bundles, and `bounded-role-conformance` consume that authority; there is no infrastructure-only spec.                       |
| Evidence, corpora, CI, and release                       | The proof ledger and `public-formats-and-artifact-lineage` establish immutable selected/executed evidence. Family extraction, patch/reproduction, runtime, and journey bundles consume private corpus receipts; `safe-runtime-proof-publication` emits only the approved derivative.       |
| Service, identity, and durable execution                 | `identity-and-access`, `privacy-and-commit-authority`, `run-policy-accounting-and-entrypoints`, and `durable-control-and-state` compose route authorization, tenancy, jobs, outbox, leases, costs, recovery, and commit-time invalidation.                                                 |
| Catalog, owned intake, structure, knowledge, and locales | `catalog-refresh-and-selection`, `locale-branch-and-policy`, `source-and-locale-knowledge`, each family profile/intake/safety bundle, and each extraction/population bundle carry acquisition through complete locale-bound structure.                                                     |
| Durable agent inner loop                                 | Shared run policy/control plus each family localization/patch/reproduction bundle carry frozen context, strict output, agent disposition, coverage, accounting, re-extraction, and resumable failure.                                                                                      |
| First production source-to-pixels vertical               | The five `admitted-*` instances for `decode.engine.reallive` form the exact intake → extraction → localized patch → runtime → played export chain. Every leg requires real bytes and the retained two-title proof.                                                                         |
| Production Studio and human rounds                       | `compatibility-and-studio-discovery`, family runtime admission, family played/export journeys, `safe-runtime-proof-publication`, and `whole-round-refinement-and-comparison` compose production routes, immutable play evidence, feedback batches, successor rounds, and export authority. |
| Benchmark, scoring, and confidence                       | `evaluation-and-confidence` owns locked comparison, calibration, replay, and non-compensating readiness; it depends on whole-round evidence rather than a separate infrastructure issue.                                                                                                   |
| Every family                                             | The admitted and new production expansions close the 16 applicable varying cells for each of 39 production targets. `bounded-role-conformance` closes only the four explicit outcomes for eight non-production rows and cannot inflate support.                                            |
| Portable platform and custody                            | `deployment-and-clean-host`, `privacy-and-commit-authority`, artifact lineage, safe proof publication, and final family journeys compose the eight-input, clean-host, portable storage/executor, retention, egress, and operator-blind contracts.                                          |

## Dependency-wave crosswalk

| Action-plan wave                   | Roadmap exit that refines it                                                                                                                                                                                                                                      |
| ---------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Concurrent promotion               | The sole root, `proof-ledger-and-explicit-failures`, executes two registry-derived promotions concurrently and fails on duplicate identity, missing input, fixed-empty output, skip, zero execution, or receipt disagreement.                                     |
| Truthful production foundation     | The shared chain through catalog, locale, knowledge, run policy, durable state, and compatibility joins with the first admitted profile/intake and extraction instances to create a tenant-safe, restart-safe project from owned bytes.                           |
| Agent-complete first-family patch  | The first admitted extraction and localization/patch/reproduction instances prove total selected scope, final accepted hashes, atomic native output, and exact re-extraction on two lawful titles.                                                                |
| Playable human round               | The first admitted runtime and played/export journey instances join safe proof publication and whole-round refinement. Causal play, route/runtime evidence, exact-patch review, successor feedback, and played export must all refer to the same immutable patch. |
| Language and portfolio composition | Locale/knowledge, run-policy, durable-state, and whole-round bundles prove three isolated locale branches and concurrent projects with disjoint output, cost, patch, and play histories.                                                                          |
| Breadth and confidence             | A later production family follows its applicable bundle chain while `evaluation-and-confidence` proves locked comparative quality, calibration, cost, latency, validity, and backlog gates.                                                                       |
| Portable private release           | Deployment, privacy, artifact lineage, safe publication, and qualified family journeys prove all placements, customer-controlled custody, endpoint and egress refusal, datastore recovery, clean lifecycle, and post-change receipt freshness.                    |

The committed discovery authority is exactly 47 canonical subjects: 39
production targets plus one synthetic reference, one benchmark reference, one
parity reference, four research-only rows, and one explicit exclusion.
[`engines.md`](engines.md) closes every alias, generation, material profile,
research lead, exclusion, and additional-scope relationship.
[`case-selection.md`](case-selection.md) fixes all 14 partial-outline mappings
and exactly 3,400 selected executable cases. The former detector-scale
hypothesis had no identity manifest and creates no additional subject.
