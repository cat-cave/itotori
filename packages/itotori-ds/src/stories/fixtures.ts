// Shared story fixtures — neutral, game-agnostic corpus (same rule as the
// gallery: a specific title is config, never baked in).

export {
  commandItems,
  costSeries,
  galleryStatuses,
  galleryToasts,
  localizationStages,
  localizationTotal,
  passLedger,
  type PassRow,
} from "../gallery/fixtures.js";
