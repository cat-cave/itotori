{
  "name": "@itotori/spec-dag-dashboard",
  "version": "0.0.0",
  "private": true,
  "bin": {
    "spec-dag-dashboard": "./dist/cli.js"
  },
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.client.json",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "vitest": "^4.1.9"
  }
}
